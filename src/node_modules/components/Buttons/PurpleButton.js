import "./PurpleButton.css";

export default function PurpleButton(props) {
  return (
    <button type={props.type} className={`button__purple ${props.className}`}>
      {props.children}
    </button>
  );
}
