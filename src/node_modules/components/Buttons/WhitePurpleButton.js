import "./WhitePurpleButton.css";

export default function WhitePurpleButton({
  className,
  type,
  children,
  onClick,
}) {
  return (
    <button
      className={`button__white-purple ${className}`}
      type={type}
      onClick={onClick}
    >
      {children}
    </button>
  );
}
