import "components/Buttons/Radio/RadioBtn.css";

export default function RadioBtn(props) {
  return (
      <label className="radio">
        <input
          type="radio"
          value={props.children}
          name={props.name}
        />
        <span>{props.children}</span>
      </label>
      
  );
}