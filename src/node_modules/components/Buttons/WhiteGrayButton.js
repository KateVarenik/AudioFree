import "./WhiteGrayButton.css";

export default function WhiteGrayButton(props) {
  return (
    <button
      onClick={props.onClick}
      className={`button__white-gray ${props.className}`}
    >
      {props.children}
    </button>
  );
}
