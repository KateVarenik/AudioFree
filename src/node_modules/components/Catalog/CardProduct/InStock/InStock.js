import "./InStock.css";
import check from "img/instock.svg";

export default function InStock(props) {
  return (
    <div className={`inStock ${props.className}`}>
      <img src={check} alt="In stock" />
      <span>В наличии</span>
    </div>
  );
}
