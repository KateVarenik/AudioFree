import "./VendorCode.css";
import CATALOG from "Consts/catalog/catalog";

export default function VendorCode({ item, className }) {
  return (
    <div className={`vendor-code ${className}`}>
      <span className="vendor-code__name">Артикул:</span>
      &nbsp;
      <span className="vendor-code__data">{item.vendorCode}</span>
    </div>
  );
}
