import "./Tabs.css";
import CATALOG from "Consts/catalog/catalog";
import DescriptionTab from "./DescriptionTab";
import CharacteristicsTab from "./CharacteristicsTab";
import CustomTabs from "components/Custom_tabs/CustomTabs";
import ReviewTab from "./ReviewTab";

export default function TabsProduct({ active, onClick }) {
  const tabs = [
    {
      label: "Описание товара:",
      content: (
        <DescriptionTab
          {...CATALOG[6].description}
          className="product__tab-description"
        />
      ),
    },
    {
      label: "Характеристики",
      content: <CharacteristicsTab {...CATALOG[6].characteristics} />,
    },
    { label: "Отзывы", content: <ReviewTab /> },
  ];

  return (
    <div className="product__tabs-box">
      <CustomTabs tabs={tabs} className="product__tabs" />
    </div>
  );
}
