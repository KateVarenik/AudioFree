import CATALOG from "Consts/catalog/catalog";

export default function DescriptionTab({ description, className }) {
  return (
    <div className={className}>
      {CATALOG[6].description.map((item, index) => (
        <p key={index}> {item}</p>
      ))}
    </div>
  );
}
