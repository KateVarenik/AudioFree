import { isMobile } from "react-device-detect";

import "./SaleSign.css";

export default function SaleSign(props) {
  return (
    <span
      className={!isMobile ? "product__sale-sign" : "product__sale-sign mobile"}
    >
      {props.children}
    </span>
  );
}
