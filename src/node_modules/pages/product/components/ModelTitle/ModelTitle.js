import "./ModelTitle.css";
import { isMobile } from "react-device-detect";

export default function ModelTitle(props) {
  return (
    <h2 className={!isMobile ? "product__model" : "product__model mobile"}>
      {props.children}
    </h2>
  );
}
